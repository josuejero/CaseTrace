# Backend Placeholder

Later phases will add the FastAPI analysis pipeline.

Planned responsibilities:

- read evidence bundles without mutating source files
- normalize records into `ArtifactRecord`
- build the case SQLite database for timeline, search, and graph views
- compare parsed output to the canonical validation dataset

