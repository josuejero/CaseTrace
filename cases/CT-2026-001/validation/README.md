# Validation Fixtures

This directory contains the canonical fixtures for the fixed Phase 0 case:

- `case_ct_2026_001_ground_truth.json` is the normalized ground-truth dataset.
- `expected_metrics.json` is the metric contract for record counts, deleted evidence totals, and minimum correlations.
